#include "toNode.h"
#include <windows.h>

node_def(_sleep){
  Sleep(args.Length() > 0 && args[0]->IsUint32()? args[0]->Uint32Value(): 0);
}

node_module_init(os){
  node_set_method("sleep", _sleep);
}